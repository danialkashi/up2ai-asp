bin/
obj/
out/
# رمز پنل و رازِ نشست — هرگز نباید وارد مخزن شود
.env
# ویرایش‌های پنل و لیدها؛ روی سرور می‌مانند، نه در گیت
data/
# فقط برای ساخت CSS روی ماشین توسعه — لازمِ اجرا نیست
node_modules/

# کش‌های ویژوال استودیو و فایل تنظیماتِ شخصیِ هر توسعه‌دهنده — نه بیلد لازمشان
# دارد نه کس دیگری. (نسخه‌ی فعلیِ مخزن ۱۹ فایل از .vs/ را دارد؛ برای بیرون
# آوردنشان یک بار `git rm -r --cached .vs src/up2ai-asp.csproj.user` لازم است.)
.vs/
*.user
